# User config — contains subscription URLs with credentials, never commit
config.yaml
config.local.yaml

# Local Clash profile — personal DNS/TUN/route data, keep out of the repo
clash.yaml

# Compiled binary
/autoconv
/autoconv.exe
/AutoConvJmsSub
/AutoConvJmsSub.exe
/autoconv-gui.exe

# Release artifacts — rebuild from source, never commit
/dist/

# GUI startup log, written next to the executable at runtime
gui-debug.log

# Go build cache / coverage
*.test
*.out
coverage.*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
